{% extends 'base.html.twig' %}

{% block title %}Candidature{% endblock %}

{% block body %}
    <h1>Candidature</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ candidature.id }}</td>
            </tr>
            <tr>
                <th>DatePostuler</th>
                <td>{{ candidature.datePostuler ? candidature.datePostuler|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>NoteQuiz</th>
                <td>{{ candidature.noteQuiz }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('candidature_index') }}">back to list</a>

    <a href="{{ path('candidature_edit', {'id': candidature.id}) }}">edit</a>

    {{ include('candidature/_delete_form.html.twig') }}
{% endblock %}
