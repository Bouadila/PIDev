{% extends 'base.html.twig' %}

{% block title %}Contrat{% endblock %}

{% block body %}
<div class="container">
    <div class=" text-center mt-5 ">
        <h2>{{ contrat.type }}</h2>
    </div>
    <table class="table">
        <tbody>
        <tr scope="row">
            <th scope="col">Description</th>
            <td scope="col">{{ contrat.description }}</td>
        </tr>
        <tr scope="row">
            <th scope="col">Type</th>
            <td scope="col">{{ contrat.type }}</td>
        </tr>
        </tbody>
    </table>

    <div class="col-md-6">
        <a href="{{ path('contrat_index') }}" class="btn btn-info backBtn btn-lg pull-left">
            <i class="fa fa-backward" aria-hidden="true"></i>
            Retourner à la list
        </a>
    </div>
    <div class="col-md-6">
        <a href="{{ path('contrat_edit', {'id': contrat.id}) }}" class="btn btn-info backBtn btn-lg pull-left">
            <i class="fa fa-pencil" aria-hidden="true"></i> Modifir
        </a>
    </div>
    <br>
    <br>
    <br>
    <div class="col-md-6">
        {{ include('contrat/_delete_form.html.twig') }}
    </div>

</div>

{% endblock %}
