{% extends 'baseBack.html.twig' %}

{% block title %}Candidature index{% endblock %}

{% block body %}
    <h1>Candidature index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id_candidat</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>Sexe</th>
                <th>Email</th>
                <th>Date_naiss</th>
                <th>Num</th>
                <th>Status</th>
                <th>Diplome</th>
                <th>Date_cand</th>
                <th>Cv</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for candidature in candidatures %}
            <tr>
                <td>{{ candidature.idCandidat }}</td>
                <td>{{ candidature.nom }}</td>
                <td>{{ candidature.prenom }}</td>
                <td>{{ candidature.sexe }}</td>
                <td>{{ candidature.email }}</td>
                <td>{{ candidature.dateNaiss ? candidature.dateNaiss|date('Y-m-d') : '' }}</td>
                <td>{{ candidature.num }}</td>
                <td>{{ candidature.status }}</td>
                <td>{{ candidature.diplome }}</td>
                <td>{{ candidature.DateCandidature ? candidature.DateCandidature|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ candidature.cv }}</td>
                <td>
                <a href="{{ path('candidatureback_show', {'id': candidature.id}) }}">show</a>
                <a href="{{ path('candidatureback_edit', {'id': candidature.id}) }}">edit</a>
                </td>
            </tr>
            
        {% else %}
            <tr>
                <td colspan="12">Aucun candidature trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('candidatureback_new') }}">Create new</a>

{% endblock %}

