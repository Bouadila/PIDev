{% extends 'base.html.twig' %}

{% block title %}Edit Offre{% endblock %}

{% block body %}
    <!-- Page Content -->
    <div class="page-heading contact-heading header-text" style="background-image: url({{asset('assets/images/heading-4-1920x500.jpg') }});">
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <div class="text-content">
                        <h4>Recrutini</h4>
                        <h2>Modifir Offre d'emploi</h2>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- Banner Ends Here -->
        {{ include('offre/_form.html.twig', {'button_label': 'Modifir'}) }}
        <br>
        <br>
    <table class="table">
        <tr scope="row">
            <td scope="col"><a href="{{ path('offre_index') }}" class="btn btn-danger backBtn btn-lg pull-left">
                    <i class="fa fa-backward" aria-hidden="true"></i> Retourner à la list
                </a>
            </td>
            <td scope="col">
                {{ include('offre/_delete_form.html.twig') }}
            </td>
        </tr>
    </table>
{% endblock %}
